#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if ! command -v ros2 >/dev/null 2>&1; then
  echo "ros2 was not found in PATH. Source your ROS/Nebula workspace first." >&2
  exit 1
fi

exec python3 "$ROOT_DIR/scripts/record_nebula_bag.py" "$@"
